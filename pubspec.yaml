name: nexierge
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.9.2


dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # State management
  flutter_riverpod: ^2.6.1
  riverpod_annotation: ^2.6.1

  # Firebase
  firebase_core: ^3.6.0
  firebase_messaging: ^15.1.3

  # Local notifications (foreground display)
  flutter_local_notifications: ^17.2.2

  # Fonts
  google_fonts: ^6.2.1

  # Realtime
  socket_io_client: ^2.0.3+1

  # Routing
  go_router: ^14.2.7

  # i18n / date formatting
  # NOTE: intl is pinned by flutter_localizations (Flutter SDK) — the upper
  # bound here must match what the current Flutter version ships, otherwise
  # `flutter pub get` fails. Bump in lockstep with `flutter upgrade`.
  intl: ^0.20.2

  # Skeleton loaders
  shimmer: ^3.0.0

  # Local persistence (theme mode etc.)
  shared_preferences: ^2.3.2

  # HTTP client for backend integration (auth, profile, tickets-when-live)
  dio: ^5.7.0

  # Secure storage for auth tokens (iOS Keychain, Android EncryptedSharedPreferences).
  # Tokens MUST NOT live in shared_preferences.
  flutter_secure_storage: ^9.2.2

  # Lucide icon glyphs (matches the React design system used by HotelOps).
  lucide_icons_flutter: ^3.0.0

  # Image picker for avatar uploads
  image_picker: ^1.1.2

  # Image compression for avatar uploads
  flutter_image_compress: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

  # Package name / bundle ID manager
  change_app_package_name: ^1.4.0

  # Riverpod code generation
  riverpod_generator: ^2.6.4
  build_runner: ^2.4.13

  # Riverpod lint rules (requires custom_lint)
  riverpod_lint: ^2.6.3
  custom_lint: ^0.7.0

  # App icon generator
  flutter_launcher_icons: ^0.14.1

  # Splash screen generator
  flutter_native_splash: ^2.4.1

# App icon configuration
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/app_logo_with_shadow.png"
  adaptive_icon_background: "#ffffff"
  adaptive_icon_foreground: "assets/images/app_logo_with_shadow.png"
  remove_alpha_ios: true

# Splash screen configuration
flutter_native_splash:
  color: "#ffffff"
  image: assets/images/app_logo_with_shadow.png
  android: true
  ios: true
  web: false

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Enables `flutter gen-l10n` driven by `l10n.yaml`. Required for the
  # generated `AppLocalizations` class consumed throughout the UI.
  generate: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # Geist (Vercel) — SIL OFL-1.1, bundled locally for design-system parity.
  # weight values follow Material's spec (100..900); italic variants via style.
  fonts:
    - family: Geist
      fonts:
        - asset: assets/fonts/Geist/Geist-Thin.ttf
          weight: 100
        - asset: assets/fonts/Geist/Geist-ThinItalic.ttf
          weight: 100
          style: italic
        - asset: assets/fonts/Geist/Geist-ExtraLight.ttf
          weight: 200
        - asset: assets/fonts/Geist/Geist-ExtraLightItalic.ttf
          weight: 200
          style: italic
        - asset: assets/fonts/Geist/Geist-Light.ttf
          weight: 300
        - asset: assets/fonts/Geist/Geist-LightItalic.ttf
          weight: 300
          style: italic
        - asset: assets/fonts/Geist/Geist-Regular.ttf
          weight: 400
        - asset: assets/fonts/Geist/Geist-Italic.ttf
          weight: 400
          style: italic
        - asset: assets/fonts/Geist/Geist-Medium.ttf
          weight: 500
        - asset: assets/fonts/Geist/Geist-MediumItalic.ttf
          weight: 500
          style: italic
        - asset: assets/fonts/Geist/Geist-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Geist/Geist-SemiBoldItalic.ttf
          weight: 600
          style: italic
        - asset: assets/fonts/Geist/Geist-Bold.ttf
          weight: 700
        - asset: assets/fonts/Geist/Geist-BoldItalic.ttf
          weight: 700
          style: italic
        - asset: assets/fonts/Geist/Geist-ExtraBold.ttf
          weight: 800
        - asset: assets/fonts/Geist/Geist-ExtraBoldItalic.ttf
          weight: 800
          style: italic
        - asset: assets/fonts/Geist/Geist-Black.ttf
          weight: 900
        - asset: assets/fonts/Geist/Geist-BlackItalic.ttf
          weight: 900
          style: italic

  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
